version: '3.8'
services:
  postgres:
    image: postgres:17
    container_name: bip-db
    environment:
      POSTGRES_DB: bip
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - ./db:/docker-entrypoint-initdb.d
